{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "$API_URL/api/$1" },
    { "source": "/uploads/(.*)", "destination": "$API_URL/uploads/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
